REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USER_AGENT=CommunityMentalHealthMonitor/1.0
PRIVACY_SALT=change_this_to_a_random_string

# Optional: LLM providers. Used by (1) pipeline evaluate for weekly briefs, (2) FastAPI POST /brief
# for the Streamlit Community Copilot. The API loads this file from the repo root automatically
# when you run `uvicorn` from `serving/`; set the same keys in Render env for production.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: cap how many recent weeks get LLM/template briefs per subreddit (default: all weeks with predictions).
WEEKLY_NARRATIVE_MAX_WEEKS=12
